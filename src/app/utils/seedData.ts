// Seed data utility - deprecated
// Backend now handles database seeding via backend/src/scripts/seed.ts
// This function is kept for backward compatibility but is no longer used

export async function initializeSeedData() {
  // Backend now handles seeding
  // This function is deprecated and no longer needed
  console.log(
    "Frontend seeding is deprecated. Backend handles database seeding.",
  );
}
